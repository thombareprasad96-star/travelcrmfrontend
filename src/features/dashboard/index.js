// src/features/dashboard/index.js
// Public API of the dashboard feature.
// (No api/ of its own — it aggregates other domains via their services
// until those features migrate, then via their @features indexes.)

export { default as Dashboard } from "./pages/Dashboard";
